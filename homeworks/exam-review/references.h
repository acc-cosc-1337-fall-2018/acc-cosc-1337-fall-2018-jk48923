#ifndef REFERENCES_H
#define REFERENCES_H
#include <string>


std::string value_reverse(std::string str_by_value);

void get_reverse(std::string str_by_reference);

void string_reference(const string str_by_reference);


#endif //REFERENCES_H