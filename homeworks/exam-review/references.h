#ifndef REFERENCES_H
#define REFERENCES_H
#include <string>

void get_reverse(std::string& str_by_reference);

std::string value_reverse(std::string& str_by_value);

void string_reference(const std::string& str_by_reference);


#endif //REFERENCES_H